# === Backend API (Cloud Run / Local) ===
PORT=8080

# Frontend URL (para CORS)
FRONT_URL=https://TU-DOMINIO.com

# JWT
JWT_SECRET=pon-un-secreto-largo

# MercadoPago
MP_PUBLIC_KEY=...
MP_ACCESS_TOKEN=...

# SMTP (emails)
SMTP_HOST=...
SMTP_PORT=587
SMTP_USER=...
SMTP_PASS=...

# === Base de datos ===
# Opción A (Cloud SQL + Cloud Run): usa socket unix
INSTANCE_CONNECTION_NAME=PROJECT:REGION:INSTANCE
DB_USER=postgres
DB_PASS=...
DB_NAME=ecommerce

# Opción B (DATABASE_URL tipo Render)
# DATABASE_URL=postgres://user:pass@host:5432/dbname

# === Storage de imágenes (reemplaza Cloudinary) ===
GCS_BUCKET=mrsmartservice-media
# opcional: carpeta/prefix dentro del bucket
GCS_UPLOAD_FOLDER=mrsmartservice
